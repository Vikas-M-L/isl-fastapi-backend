Place your models here:

- model1_savedmodel/ (TensorFlow SavedModel directory, must contain saved_model.pb)
- model2_savedmodel/ (TensorFlow SavedModel directory)
- labels.json (index-to-label mapping like {"0": "hello", "1": "thank you"})

You can override these paths via .env using MODEL1_PATH, MODEL2_PATH, and LABELS_PATH.
