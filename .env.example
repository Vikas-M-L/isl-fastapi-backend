# Copy to .env and adjust paths as needed
MODEL1_PATH=models/model1_savedmodel
MODEL2_PATH=models/model2_savedmodel
LABELS_PATH=models/labels.json

# Optional Gemini API key (leave blank if not using Gemini)
GOOGLE_API_KEY=

# Allowed frontend origins (comma-separated)
FRONTEND_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# De-dup cooldown seconds for repeated predictions
DEDUP_COOLDOWN=2.0
